---
/**
 * Light/dark toggle. Cycles the `data-theme` attribute set pre-paint by the
 * inline script in BaseLayout; the choice persists in localStorage.
 */
---

<button id="theme-toggle" type="button" aria-label="Toggle color theme">
  <svg
    class="icon-sun"
    width="18"
    height="18"
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    stroke-width="2"
    stroke-linecap="round"
    aria-hidden="true"
  >
    <circle cx="12" cy="12" r="4"></circle>
    <path
      d="M12 2v2m0 16v2M4.9 4.9l1.4 1.4m11.4 11.4 1.4 1.4M2 12h2m16 0h2M4.9 19.1l1.4-1.4m11.4-11.4 1.4-1.4"
    ></path>
  </svg>
  <svg
    class="icon-moon"
    width="18"
    height="18"
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    stroke-width="2"
    stroke-linecap="round"
    stroke-linejoin="round"
    aria-hidden="true"
  >
    <path d="M21 12.8A9 9 0 1 1 11.2 3a7 7 0 0 0 9.8 9.8Z"></path>
  </svg>
</button>

<style>
  button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.25rem;
    height: 2.25rem;
    padding: 0;
    background: none;
    border: 1px solid var(--border-strong);
    border-radius: var(--radius);
    color: var(--text-muted);
    cursor: pointer;
  }

  button:hover {
    color: var(--text);
    border-color: var(--text-muted);
  }

  .icon-moon {
    display: none;
  }

  /* show the icon for the currently active theme */
  :global([data-theme='dark']) .icon-sun {
    display: none;
  }

  :global([data-theme='dark']) .icon-moon {
    display: block;
  }

  @media (prefers-color-scheme: dark) {
    :global(:root:not([data-theme='light'])) .icon-sun {
      display: none;
    }

    :global(:root:not([data-theme='light'])) .icon-moon {
      display: block;
    }
  }
</style>

<script>
  const toggle = document.getElementById('theme-toggle');
  toggle?.addEventListener('click', () => {
    const root = document.documentElement;
    const systemDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
    const current = root.dataset.theme ?? (systemDark ? 'dark' : 'light');
    const next = current === 'dark' ? 'light' : 'dark';
    root.dataset.theme = next;
    /* The switch itself is above and has already happened, so a browser
       that refuses storage still gets the theme it asked for — it just
       will not be remembered. Guarded because `setItem` throws outright
       where site data is blocked, and an uncaught throw on the one control
       every page carries is not a good look in a console. */
    try {
      localStorage.setItem('theme', next);
    } catch {
      /* this visit only, then */
    }
  });
</script>
